# TechInspector

A Real-time System Telemetry and Storage Diagnostics Dashboard built with Python (Flask) and Bootstrap.

## Features
- Live CPU and RAM load tracking
- Hardware-level physical storage details
- Interactive UI with Dark/Light theme toggle

## How to Run
1. Install dependencies: `pip install -r requirements.txt`
2. Run the application: `python app.py`